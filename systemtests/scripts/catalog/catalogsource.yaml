apiVersion: operators.coreos.com/v1alpha1
kind: CatalogSource
metadata:
  name: ${CATALOG_NAME}
  namespace: ${CATALOG_NAMESPACE}
spec:
  sourceType: grpc
  image: ${INDEX_IMAGE}
  displayName: StreamsHub Console Operator Catalog
  publisher: StreamsHub
